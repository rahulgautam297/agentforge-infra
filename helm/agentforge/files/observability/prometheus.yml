global:
  scrape_interval: 5s

scrape_configs:
  - job_name: execution-platform
    static_configs:
      - targets: ['{{ include "agentforge.fullname" . }}-execution-platform:8001']
    metrics_path: /metrics

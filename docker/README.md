# docker

**Phase:** 1

Docker Compose stack for local development: single TimescaleDB container
for the shared `agentforge` database, OpenSearch, Temporal dev-server,
Prometheus, Grafana, mock MCP tool servers, MockProvider — brings up the
whole platform with zero AWS credentials required.
